{
	"name": "@bts/agentcore",
	"scripts": {
		"preinstall": "uv sync",
		"format": "uv run ruff format",
		"lint": "uv run ruff check --fix",
		"check": "uv run ruff check",
		"prod-deploy": "./deploy.sh",
		"prod-remove": "uv run agentcore destroy",
		"get-info": "node get-deployment-info.js"
	}
}
